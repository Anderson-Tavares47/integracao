name: Test Secret Access

on:
  push:
    branches:
      - main  # O workflow será executado ao fazer push para a branch main

jobs:
  test-secrets:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '16'

      - name: Test access to GitHub Secret
        env:
          SECRET_DATA: ${{ secrets.SECRET_DATA }}
        run: echo "SECRET_DATA is $SECRET_DATA"
